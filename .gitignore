# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local env vars — never commit
.env

# Build output — regenerated by Netlify on each deploy
site/

# Generated at build time from ADMIN_EMAIL env var (inside site/)
# (covered by site/ above, but listed explicitly as a reminder)
# site/admin/config.js
